#include<iostream>
#include<vector>
#include<string>

using namespace std;

int main()
{
    int T;
    cin >> T;
    for(int i = 0; i < T; ++i) {
	int n;
	string b;
	int l = b.size()
	cin >> n >> b;
	vector<<vector<int>> P (n+1, vector<int> (l));
	
    }
}
