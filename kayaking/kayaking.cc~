#include<iostream>

using namespace std;
typedef long long ll;

ll sf[100005];

int main()
{
    ll b, n, e;
    cin >> b >> n >> e;
    ll sb, sn, se;
    cin >> sb >> sn >> se;
    for(int i = 0; i < (b+n+e) / 2; ++i) {
	cin >> sf[i];
    }
    ll lo = 0, hi = 1e9+5, mx = 0;
    while(lo <= hi) {
	if(poss
    }
}
