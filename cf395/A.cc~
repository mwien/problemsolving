#include<iostream>

using namespace std;

int main()
{
    int n, m, z;
    cin >> n >> m >> z;
    int cnt = 0;
    for(int i = 1; i <= z; ++i) {
	if(z % n == 0 && z % m == 0) ++cnt;
    }
    cout << cnt << endl;
}
