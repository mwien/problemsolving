#include<iostream>
#include<algorithm>

using namespace std;

string s, st;

int main()
{
    int g;
    cin >> g;
    for(int i = 0; i < g; ++i) {
	cin >> s;
	st = s;
	sort(st.begin(), st.end());
	
    }
}
