#include<iostream>
#include<vector>

using namespace std;

int a[131072];
int res[131072];

vector<int> karatsuba(int lower, int upper) {
  if(upper - lower == 1)  
    return
}

int main()
{
  int N;
  cin >> N;
  for(int i = 0; i < N; ++i) {
    int ind;
    cin >> ind;
    ++a[ind+50000];
  }
}
