#include<iostream>

using namespace std;

char m[1005][1005];

int main()
{
    int w, h;
    cin >> w >> h;
    for(int i = 0; i < h; ++i) {
	string s;
	cin >> s;
	for(int j = 0; j < w; ++j) {
	    m[i][j] = s[j];
	}
    }
}
