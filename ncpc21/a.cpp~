#include<iostream>

using namespace std;

int main()
{
    int n, c;
    cin >> n >> c;
    int mn = 1e9;
    for(int i = 0; i < n; ++i) {
	int a;
	cin >> a;
	mn -= c;
	mn = min(mn, a);
	cout << a - mn << endl;
    }
}
