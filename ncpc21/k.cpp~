#include<iostream>

using namespace std;

int main()
{
    int n;
    cin >> n;
    set<int> s;
    for(int i = 0; i < n; ++i) {
	int a;
	cin >> a;
	s.insert(a);
    }
    for(int i = 0; i < n; ++i) {
	int a;
	cin >> a;
	s.remove(a);
    }
    for(int i : s) {
	cout << i << endl;
    }
}
