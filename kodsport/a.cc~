#include<iostream>

using namespace std;

int w[1000005];
int t[1000005];
int r = 0;
int rt = -1;

int main()
{
    int n, q;
    cin >> n >> q;
    for(int i = 0; i < q; ++i) {
	string a;
	cin >> a;
	if(a == "SET") {

	}
	if(a == "PRINT") {

	}
	if(a == "RESTART") {
	    
	}
    }
}
