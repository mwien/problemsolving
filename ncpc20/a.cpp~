#include<iostream>

using namespace std;

string a[105];
long long b[105];

long long tenth(int a)
{
    long long res = 1;
    for(int i = 0; i < a; ++i) {
	res *= 10;
    }
    return res;
}

int main()
{
    int n;
    cin >> n;
    for(int i = 0; i < n; ++i) {
	cin >> a[i];
    }
    for(int i = 0; i < n; ++i) {
	if(i > 0) {
	    
	}
	if(i < n-1) {
	    for(int j = 0; j < n; ++j) {
		b[j] = a[j];
		b[i][0] = '9';
	    }
	}
    }
}
