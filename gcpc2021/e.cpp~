#include<iostream>

using namespace std;

pair<int, pair<int, int>> edges[100005];

int main()
{
    int n,m;
    cin >> n >> m;

    for(int i = 0; i < m; ++i) {
	int a, d, c;
	cin >> a >> d >> c;
	edges[i] = {a, {d, c}};
    }
}

