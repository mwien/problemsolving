#include<iostream>

using namespace std;

int main()
{
    int n, m, q;
    cin >> n >> m >> q;
    vector<vector<int>> queries;
    for(int i = 0; i < q; ++i) {
	int x1, y1, x2, y2;
	cin >> x1 >> y1 >> x2 >> y2;
	queries.push_back({x1, y1, x2, y2});
    }
}
