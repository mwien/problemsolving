#include<iostream>

using namespace std;

int gcd(int a, int b)
{
    while(b > 0) {
	int tmp = b;
	b = a % b;
	a = tmp;
    }
    return a;
}

int main()
{
    int n, m, c;
    cin >> n >> m >> c;
    for(int i = 0; i < m; ++i) {
	
    }
}
