#include<iostream>

using namespace std;

bool eq(j) {
    
}

int main()
{
    for(int i = 2; i <= 24; i += 2) {
	int cnt = 0;
	for(int j = 0; j < (1 << i); ++j) {
	    if(eq(j) || con(j)) {
		++cnt;
	    }
	}
    }
}
