#include<iostream>

using namespace std;

int l[1005];
int r[1005];
int g[2005];

int main()
{
  int n;
  cin >> n;
  for(int i = 0; i < n; ++i) {
    cin >> l[i] >> r[i];
  }
  int p;
  cin >> p;
  for(int i = 0; i < p; ++i) {
    cin >> g[i];
  }
  
}
