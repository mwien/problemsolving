#include<iostream>
#include<vector>

using namespace std;

int main()
{
    int N;
    long long K;
    cin << N << K;
    vector<int> F (N+1);
    F[1] = 1;
    F[2] = 1;
    for(int i = 3; i < N+1; ++i) {
	if(F[i-1] == -1 && F[i-2] == -1) {
	    F[i] = -1;
	    continue;
	}
	
    }
}
