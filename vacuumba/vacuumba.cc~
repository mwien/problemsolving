#include<iostream>
#include<complex>

using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;
    double old_angle = 0;
    complex<double> pos(0.0, 0.0);
    for(int i = 0; i < m; ++i) {
	double angle, dist;
	cin >> angle >> dist;
	
    }
}
