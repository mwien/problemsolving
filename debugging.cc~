#include<iostream>

using namespace std;

int l[1000005];

int recdeb(int ls) {
    int min = (1 << 30);
    for(int i = 0; i <= n; i++) {
	
    }
}

int main()
{
    int n, r, p;
    cin >> n >> r >> p;
    for(int i = 0; i <= n; i++) {
	l[i] = -1;
    }
}
