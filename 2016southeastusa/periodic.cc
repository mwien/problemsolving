#include<iostream>
#include<string>

using namespace std;

int main()
{
    string s;
    cin >> s;
    int n = s.size();
    for(int i = 1; i < n; ++i) {
	if(n % i != 0) continue;
	bool succ = true;
	for(int j = 1; j < ; ++j) {
	    if(s[(j-1)*
	}
    }
}
