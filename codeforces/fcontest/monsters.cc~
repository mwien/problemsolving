#include<iostream>
#include<vector>

using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> a;
    for(int i = 0; i < n; ++i) {
	int cur;
	cin >> cur;
	a.push_back(cur);
    }
    int k;
    cin >> k;
    vector<int> b;
    for(int i = 0; i < k; ++i) {
	int cur;
	cin >> cur;
	b.push_back(cur);
    }
}
