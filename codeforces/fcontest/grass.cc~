#include<iostream>
#include<string>

using namespace std;

int main()
{
    string in;
    cin >> in;
    int pos = -1;
    vector<char> vowels = {'A', 'E', 'I', 'O', 'U', 'Y'};
    for(int i = 0; i < in.size(); ++i) {
	
    }
}
