#include<iostream>

using namespace std;

long long p[10005];

int main()
{
    int N;
    while(true) {
	cin >> N;
	if(N == 0) break;
	for(int i = 0; i < N; i++) {
	    double cur;
	    cin >> cur;
	    cur *= 1000;
	    p[i] = round(cur);
	}
	
    }
}
