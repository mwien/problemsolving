#include<iostream>
#include<string>

using namespace std;

int main()
{
    int T;
    cin >> T;
    for(int tc = 0; tc < T; ++tc) {
	int H, S;
	cin >> H;
	cin >> S;
	double max = 0;
	for(int i = 0; i < S; ++i) {
	    string spell;
	    cin >> spell;
	    int X, Y, Z;
	    for(int i = 0; i < spell.size(); ++i) {
		if(spell[i] == 'd') {
		    X = stoi(spell.substr(0, i+1));
		}
	    }
	}
    }
}
