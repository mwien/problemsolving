#include<iostream>
#include<multiset>

using namespace std;

int main()
{
    int T;
    cin >> T;
    for(int tc = 0; tc < T; ++tc) {
	int N;
	cin >> N;
	multiset<int> items;
	for(int i = 0; i < N; ++i) {
	    int a;
	    cin >> a;
	    items.insert(a);
	}
    }
}
