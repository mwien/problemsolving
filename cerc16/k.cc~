#include<iostream>
#include<string>

using namespace std;

int main()
{
    string s;
    cin >> s;
    int w = 1;
    for(int i = 1; i < s.size(); ++i) {
	if(s[i] != s[i-1]) ++w;
    }
    for(int i = 3; i < s.size(); ++i) {
	
    }
}
