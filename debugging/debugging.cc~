#include<iostream>

using namespace std;

int l[1000005];

int recdeb(int ls) {
    if(ls == 1) return 0;
    int min = (1 << 30);
    for(int i = 1; i < n; i++) {
	int bslice = n / i;
	if(n % i != 0) ++bslice;
	int nmin = (1 << 30);
	if(l[i] != -1) {
	    nmin = l[i] //TODO
	} else {
	    
	}
    }
}

int main()
{
    int n, r, p;
    cin >> n >> r >> p;
    for(int i = 0; i <= n; i++) {
	l[i] = -1;
    }
}
